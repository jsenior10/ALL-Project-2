command to run because of libraries

sudp apt-get install lib sqlite3  //to use #include <sqlite3.h> (not sure is working)
sudo apt-get install libsqlite3-dev


sudo apt install libncurses5-dev


sudo apt-get install ncurses-dev

